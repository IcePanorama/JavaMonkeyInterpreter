package ast;

public interface Expression extends Node {
    /*
     * This method is unused by the interpreter,
     * but I'm going to keep it around in case 
     * it ends up being used for one of the other
     * projects in this book series.
     */
    void expressionNode();
}